{% extends 'base.html.twig' %}

{% block navbar %}
    <div class="navbar">
        <h1 class="navbar-title">My résumé</h1>
        
        <div class="edit-url-group">
            <input class="edit-url" id="cv-url" value="http://redefin.io/erikas-malisauskas" readonly>
            <button class="btn edit-url-btn">Copy</button>
            <span class="label label-success text-copied">Copied!
                <img src="/img/check.png" alt="Icon">
            </span>
        </div>
       
        <div class="navbar-actions">
            <a href="{{ path('cv_edit', { 'id': cV.id }) }}" class="btn navbar-edit">Edit</a>
            <a href="{{ path('cv_pdf') }}" class="btn navbar-download">Download PDF</a>
        </div>
    </div>
{% endblock %}

{% block body %}
	<div id="template" class="resume-container">
		<div id="loader" class="active">
			<div class="signal"></div>
		</div>
	</div>
{% endblock %}

{% block javascripts %}
		<script type="text/javascript">
			var templateUrl = "{{ path('cv_index') }}";
			var isEditing = false;
            var templateId = {{ cV.getPublicTemplate().getId() }};
		</script>
  <script type="text/javascript" src="{{ asset('js/app.js') }}"></script>
{% endblock %}